<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pro.referral">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Referral">
        <activity android:name=".rozarPay"></activity>
        <activity android:name=".helpPage" />
        <activity android:name=".kyc_verify" />
        <activity android:name=".editProDetails" />
        <activity android:name=".orderHistoryDetails" />
        <activity android:name=".common" />
        <activity
            android:name=".choice"
            android:label="@string/title_activity_choice" />
        <activity android:name=".details" />
        <activity android:name=".MainActivityVerify" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>